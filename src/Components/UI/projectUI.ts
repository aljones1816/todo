// a div with a single project card
// clicking project card should filter todos to only those that belong to that project
// card needs a button to delete the project
// card needs a button to edit the project name
